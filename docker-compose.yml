services:
  task-api:
    build: .
    container_name: task-api-container
    ports:
      - "8000:8000"
    environment:
      APP_ENV: ${APP_ENV}
      DATABASE_URL: ${DATABASE_URL}
    volumes:
      - task-api-data:/app/data

volumes:
  task-api-data: